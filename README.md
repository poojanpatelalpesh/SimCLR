# SimCLR
SimCLR implementation from scratch in PyTorch for self-supervised visual representation learning. Trained on CIFAR-10 using contrastive learning and evaluated via linear probing to demonstrate learned semantic features without labels.
